# Architecture Notes

本文记录 `trans` 在“是否使用 Claude Code 作为翻译引擎”以及“是否需要自研 agent”上的设计判断。

## 结论

当前阶段，继续使用 Claude Code 作为翻译引擎是更合理的选择。

`trans` 暂时没有必要自研一个完整 agent。更值得投入的方向是：

- 稳定翻译工作流
- 提升增量翻译能力
- 固化任务输入输出协议
- 沉淀 terminology / style / domain 等知识资产
- 为未来执行层替换预留接口

## 为什么当前适合继续使用 Claude Code

### 1. Claude Code 的综合能力很难在短期内被自研替代

Claude Code 的价值不只是模型能力，还包括一整套成熟的 agent 执行能力，例如：

- 多文件上下文组织
- 文件读取和工具调用
- 长上下文中的信息筛选
- 执行过程中的错误恢复
- 已有的 CLI 交互与自动化调用方式

如果自研 agent，最常见的结果不是“做出更强的系统”，而是“做出一个能跑但整体能力明显更弱的版本”。

### 2. `trans` 的核心价值不在 agent 本身

`trans` 当前真正有价值的部分是翻译工作流，而不是通用 agent runtime：

- i18n 文件扫描
- flatten / unflatten
- 增量 diff
- snapshot 与历史翻译缓存
- `task.json` / `result.json` 协议
- `.claude/skills/` 中的术语、风格和领域知识沉淀

这些能力和底层执行器是可以解耦的。

换句话说，`trans` 应该重点建设的是“翻译系统”，不是重新发明一个“通用 agent”。

### 3. 自研 agent 的复杂度比表面高很多

从表面看，一个 agent 好像只是“调用模型 + 调工具 + 循环执行”。但真正要把它做到稳定可用，复杂度会迅速上升，包括：

- 上下文裁剪和排序
- 工具选择策略
- 多轮失败重试
- 结构化输出约束
- 日志与可观测性
- token 和成本控制
- 安全边界与 prompt 注入问题
- 不同项目结构下的稳定性

这些问题单独看都不小，组合起来就是一个长期维护成本很高的系统工程。

## 使用 Claude Code 作为翻译引擎的优势

### 1. 能快速验证产品价值

采用 Claude Code 可以让 `trans` 先把精力集中在真正重要的用户价值上，而不是把时间花在搭建底层 agent 基础设施上。

### 2. 翻译质量上限更高

Claude Code 本身具备较强的上下文理解和多文件推理能力，对翻译这种需要结合术语、风格、历史翻译和项目上下文的任务更有优势。

### 3. 能直接复用技能沉淀

当前设计里的 `.claude/skills/terminology.md`、`.claude/skills/style.md`、`.claude/skills/domain.md` 都可以直接被 Claude Code 消费，而不需要额外再做一套知识加载系统。

### 4. 工程复杂度更低

`trans` 只需要负责：

- 扫描文件
- 生成任务
- 调用执行器
- 解析结果并回写

这样架构边界清楚，也更容易维护。

### 5. 可随着底层能力升级而受益

如果 Claude Code 的模型能力、工具调用、上下文策略继续变强，`trans` 可以直接受益，而不需要自己承担升级成本。

## 使用 Claude Code 的代价和限制

### 1. 对执行过程的控制力有限

使用外部 agent 意味着：

- 对中间推理步骤控制有限
- 对工具调度策略控制有限
- 对输出稳定性依赖 CLI 行为

这会影响某些场景下的可预测性。

### 2. 对外部工具存在依赖

当前使用方式依赖本地 Claude Code CLI，包括：

- 安装
- 登录认证
- CLI 参数行为
- 权限模式
- 输出格式

如果这些行为发生变化，`trans` 需要适配。

### 3. 可移植性一般

对于纯服务端运行、批处理、CI/CD、企业内网或模型供应商切换等场景，直接依赖 Claude Code 可能会变成限制。

## 是否有必要自研 agent

当前没有必要。

更准确地说，当前没有必要自研“完整通用 agent”。

真正值得做的是把执行层抽象出来，让底层执行器可替换。

## 更合理的方向：先设计可替换执行层

建议把 `trans` 的架构拆成两层：

### 1. `trans` 自己维护的稳定层

这部分是项目真正的核心资产：

- scan
- diff
- snapshot
- cache
- task / result 协议
- merge / writeback
- skills 的组织和约束

### 2. 可插拔执行器层

这部分负责真正调用底层翻译引擎，例如：

- `ClaudeCodeExecutor`
- `APIExecutor`
- `CustomAgentExecutor`

这样做的好处是：

- 当前可以继续用 Claude Code
- 未来如果要切换到底层 API，不需要重做整个系统
- 如果某天确实要自研 agent，也只是在执行器层扩展，而不是推翻全部设计

## 什么情况下才值得考虑自研 agent

只有在下面这些条件明确成立时，才值得认真评估：

- Claude Code 无法满足部署要求
- 成本不可控
- 输出稳定性长期无法接受
- 需要更严格的结构化输出
- 需要自定义多阶段翻译流程
- 需要模型供应商无关能力
- 需要在服务端完全托管，不依赖本地 CLI

在这些问题没有成为真实瓶颈之前，自研 agent 很容易变成过早优化。

## 当前建议

对 `trans` 而言，当前阶段最合理的路线是：

1. 继续使用 Claude Code 作为翻译引擎
2. 把 `task.json` / `result.json` 协议稳定下来
3. 提升错误处理、日志和可观测性
4. 持续积累 terminology / style / domain
5. 把 Claude 调用封装为执行器接口，为未来替换底层做准备

## 一句话总结

如果目标是做一个好用的翻译工具，不应该急着自己造 agent。

如果未来目标变成做一个完全可控、可部署、与 Claude Code 解耦的 agent 平台，那时再考虑自研才合理。
